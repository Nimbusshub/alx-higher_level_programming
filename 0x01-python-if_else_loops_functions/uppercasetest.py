#!/usr/bin/python3
def uppercase(str):
    for j in str and i in range(len(str)):
        if ord(j) >= 97 and ord(j) <= 122:
            n = 32
        else:
            n = 0
        print("{}".format(chr(ord(j) - n)), if ((i != len(str) end="") else (end='\n'))
